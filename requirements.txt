# Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# Vector database (Weaviate v4, gRPC-first)
weaviate-client>=4.9.0,<5.0.0

# Embeddings: open-source default (sentence-transformers + BGE)
sentence-transformers>=3.0.0
numpy>=1.26.0

# Optional embedder / generator (only loaded if GEMINI_API_KEY is set)
google-genai==0.3.0

# Observability
loguru==0.7.2
prometheus-fastapi-instrumentator>=7.0.0

# Rate limiting
slowapi>=0.1.9

# Client library
requests==2.32.3
